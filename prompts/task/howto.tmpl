Write the how-to guide "${PAGE_TITLE}" (slug: ${PAGE_SLUG}) for the repository "${WORKSPACE_NAME}".

Why this page is in the plan: ${PAGE_RATIONALE}
Audience: ${PAGE_AUDIENCE}

Source files to ground the guide in (paths relative to the repository root; read them with the Read tool):

${SOURCE_PATHS}

Other pages in the documentation plan. Use these slugs for `assumes` and `related` links; link to reference pages for detail and explanation pages for why:

${PLAN_PAGES}

Remember: exactly one goal. If this page actually contains more than one goal, return the split object instead of a page.

Return the complete guide as structured output.
