Audit the documentation page at "${AUDIT_PAGE_PATH}" (path relative to the repository root; read it with the Read tool).

The page claims (or is located as) Diataxis mode: ${AUDIT_PAGE_MODE}

Executable languages configured for this repository (relevant to the unverified_code rule): ${EXECUTABLE_LANGUAGES}

Apply the rubric from your instructions against this one page. Verify citation targets with Read, Grep or Glob before reporting broken_citation. Return findings as structured output; return an empty findings array if the page is clean.
