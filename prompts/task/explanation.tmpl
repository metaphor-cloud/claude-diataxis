Write the explanation page "${PAGE_TITLE}" (slug: ${PAGE_SLUG}) for the repository "${WORKSPACE_NAME}".

Why this page is in the plan: ${PAGE_RATIONALE}
Audience: ${PAGE_AUDIENCE}

Source files to ground the page in (paths relative to the repository root; read them with the Read tool). Also consult the source tree shape, ADRs if present, the dependency manifest, and recent git history via the tools:

${SOURCE_PATHS}

Other pages in the documentation plan. Use these slugs for `related` links:

${PLAN_PAGES}

Return the complete page as structured output.
